import styled from 'styled-components'

// Styled components
const StyledMenuTableDiv = styled.div`
  display: table;
  width: 100%; // Ensures the table div takes full width
`

const StyledMenuTableCellDiv = styled.div`
  width: 90%; // Uses 90% of the parent's width
  display: table-cell;
`

export {StyledMenuTableDiv, StyledMenuTableCellDiv}
